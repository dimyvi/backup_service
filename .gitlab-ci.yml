stages:
  - test
  - build

test:
  image: python:3.12
  stage: test
  script:
    - pip install -r requirements.txt
    - pytest

build:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  script:
    - docker build -t backup_service .